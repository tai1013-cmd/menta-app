<!--?xml version="1.0" encoding="UTF-8" ? -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="menta.app.dao.recruitMentor.RecruitMentorDao">
  <select id="selectList" resultType="menta.app.dao.recruitMentor.dataModel.RecruitMentorDataModel">
    select * 
    from recruitMentors
    order by createdAt desc
  </select>
  
  <select id="selectListByUserId" resultType="menta.app.dao.recruitMentor.dataModel.RecruitMentorDataModel">
    select * 
    from recruitMentors
    where referUserId = #{referUserId}
    order by createdAt desc
  </select>

  <select id="selectOneByRecruitMentorId" resultType="menta.app.dao.recruitMentor.dataModel.RecruitMentorDataModel">
    select * 
    from recruitMentors 
    where id = #{recruitMentorId}
  </select>
  
  <insert id="insert">
    insert into recruitMentors(
    id,
    title,
    detail,
    budget,
    status,
    type,
    referUserId,
    createdAt,
    updatedAt
    )
    values(
    #{id},
    #{title},
    #{detail},
    #{budget},
    #{status},
    #{type},
    #{referUserId},
    #{createdAt},
    #{updatedAt}
    )
  </insert>
  
  <update id="update">
    update recruitMentors set
    title = #{title},
    detail = #{detail},
    budget = #{budget},
    status = #{status},
    type = #{type},
    referUserId = #{referUserId},
    updatedAt = #{updatedAt}
    where id = #{id}
  </update>
  
  <delete id="delete">
    delete
    from recruitMentors
    where id = #{recruitMentorId}
  </delete>
  
</mapper>

